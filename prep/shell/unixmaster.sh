#!/bin/bash


# simple usage of all

sed 's/pizza/cookies/g' <(echo "the pizza man delivers twice a day. pizza.")

# sort -n -u (unique numerics) -c (strict unique)
# doesn't always behave the way I expect
# Example: how to sort by the end?

# nvm sort on word
#cat /proc/cpuinfo | grep processor | sort -ns


# for awk just read the book boss
